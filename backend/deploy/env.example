# /opt/stall-booker/.env
# Copy this file, fill in real values, chmod 600 .env, chown stallbooker:stallbooker .env

# Database
DB_URL=jdbc:mysql://127.0.0.1:3306/stall_booker?useSSL=true&serverTimezone=Asia/Kolkata
DB_USERNAME=stallbooker_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# JWT (generate: openssl rand -base64 64)
JWT_SECRET=CHANGE_ME_64_CHAR_BASE64_SECRET

# AWS
AWS_ACCESS_KEY=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_REGION=ap-south-1
AWS_S3_BUCKET=stall-booker-prod

# Mail (AWS SES SMTP)
MAIL_HOST=email-smtp.ap-south-1.amazonaws.com
MAIL_PORT=587
MAIL_USERNAME=SES_SMTP_USERNAME
MAIL_PASSWORD=SES_SMTP_PASSWORD

# CORS — your frontend domain(s)
APP_CORS_ALLOWED_ORIGINS=https://amrutpeth.com,https://www.amrutpeth.com
